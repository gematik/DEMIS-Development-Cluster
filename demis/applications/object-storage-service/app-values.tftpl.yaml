# Values for object-storage-service

istio:
  enable: ${istio_enable}

required:
  image: 
    repository: ${repository}
    %{ if strcontains(repository, "gematik1") }
    name: demis-object-storage-service
    %{ endif }

# Image Pull Secrets
imagePullSecrets: 
  %{~ for pull_secret in image_pull_secrets ~}
  - name: ${pull_secret}
  %{~ endfor ~}

# podAnnotations: 
#  checksum/object-storage-service-secret: "${object_storage_service_secret_checksum}"
  
# Define custom configuration values
# The Entries will be read from the given (external) Secrets and injected in the container with the same name.
# IMPORTANT: these values must be defined in the Secret Objects
config:
  useDefault: true
  settings:
    configMap:
      name: object-storage-service-config
  # Defines if object-storage-service data must be persisted on disk
  persistence:
    enabled: true
    # The Persistence Volume Claim where the files are available
    volumeClaim:
      # Use default Persistence Volume Claim from this Chart
      useDefault: false
      # The (external) Persistence Volume Claim to be used
      name: object-storage-volume-claim
    # The Persistence Volume where to store the data
    volume: 
      # Use the default embedded Persistence Volume (hostpath, for localhost)
      useDefault: false
  # The (external) Secret Objects containing the API Key entry
  secrets:
    # Use default Secret from this Chart
    useDefault: false
    # The (external) Secret for object-storage-service Keys
    s3:
      # The name of the (external) Secret for object-storage-service and the entries to be used and mapped
      name: object-storage-service-secret
      entries:
        S3_ACCESS_KEY: S3_ACCESS_KEY
        S3_SECRET_KEY: S3_SECRET_KEY

customEnvVars:
  # Prevents empty values that can delete the default environment variables
  TERRAFORM_DEPLOYMENT: true
  %{~ for flag_name, flag_value in feature_flags ~}
  ${flag_name} : ${flag_value}
  %{~ endfor ~}
  %{~ for config_name, config_value in config_options ~}
  ${config_name} : "${config_value}"
  %{~ endfor ~}

podAnnotations:
  sidecar.istio.io/proxyCPU: ${istio_proxy_resources.requests.cpu}
  sidecar.istio.io/proxyMemory: ${istio_proxy_resources.requests.memory}
  sidecar.istio.io/proxyMemoryLimit: ${istio_proxy_resources.limits.memory}

%{~ if replica_count != null ~}
# Number of Pod Replicas desired
replicaCount: ${replica_count}
%{endif}

%{~ if resource_block != null ~}
# Override Resource Definitions for Pod
${resource_block}
%{endif}
